# Infrastructure-build



## Usage

We will create the infrastructure itself in this project. We create an infrastructure that can run any application that is running in the container. The created virtual machines, will use the CentOS7 operating system and their creation will be automated within the CI/CD pipeline in collaboration with Terraform and the objects created in AWS for the backend.
